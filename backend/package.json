{
  "name": "backend",
  "version": "1.0.0",
  "description": "A backend for email bounce check",
  "keywords": [
    "node",
    "mongo",
    "bounce",
    "risky"
  ],
  "license": "ISC",
  "author": "Manish Sahani",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env src/server.js",
    "dev": "node --watch --env-file=.env src/server.js"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "dns": "^0.2.2",
    "express": "^5.2.1",
    "validator": "^13.15.26"
  }
}
